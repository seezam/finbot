# 🔒 КРИТИЧЕСКИ ВАЖНО: Отзыв скомпрометированного токена

## ⚠️ Токен бота был закоммичен в публичный репозиторий!

Токен `REDACTED_TELEGRAM_BOT_TOKEN` был обнаружен в истории Git и должен быть немедленно отозван.

## 🚨 Немедленные действия:

### 1. Отзовите токен через BotFather

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду: `/revoke`
3. Выберите вашего бота из списка
4. Подтвердите отзыв токена
5. Сгенерируйте новый токен командой: `/token`
6. **Сохраните новый токен в безопасном месте**

### 2. Обновите переменные окружения в Railway

1. Откройте ваш проект на Railway
2. Перейдите в **Settings → Variables**
3. Обновите `BOT_TOKEN` на новый токен
4. Railway автоматически перезапустит приложение

### 3. Обновите локальный .env файл

```bash
# Отредактируйте .env файл
BOT_TOKEN=НОВЫЙ_ТОКЕН_ОТ_BOTFATHER
ALLOWED_USER_ID=7186109787
PORT=3000
```

### 4. Обновите webhook с новым токеном

```bash
export BOT_TOKEN="НОВЫЙ_ТОКЕН_ОТ_BOTFATHER"
./check-webhook.sh https://finbot-production-19c7.up.railway.app
```

## 📝 Что было исправлено:

- ✅ Токен заменен на placeholder во всех файлах
- ✅ Скрипты обновлены для использования переменных окружения
- ⚠️ **Требуется очистка истории Git** (см. ниже)

## 🧹 Очистка истории Git (ВАЖНО!)

Токен все еще присутствует в истории коммитов. Для полной очистки:

### Вариант 1: Использовать git filter-repo (рекомендуется)

```bash
# Установите git-filter-repo
pip install git-filter-repo

# Очистите историю от токена
git filter-repo --replace-text <(echo "REDACTED_TELEGRAM_BOT_TOKEN==>YOUR_BOT_TOKEN_HERE")

# Force push (ОСТОРОЖНО! Это перезапишет историю)
git push origin --force --all
git push origin --force --tags
```

### Вариант 2: Использовать BFG Repo-Cleaner

```bash
# Скачайте BFG: https://rtyley.github.io/bfg-repo-cleaner/
# Создайте файл tokens.txt с токеном
echo "REDACTED_TELEGRAM_BOT_TOKEN" > tokens.txt

# Очистите историю
java -jar bfg.jar --replace-text tokens.txt

# Force push
git push origin --force --all
```

### ⚠️ ВАЖНО перед force push:

1. Убедитесь, что все участники проекта знают о force push
2. Сделайте backup репозитория
3. После force push все должны сделать:
   ```bash
   git fetch origin
   git reset --hard origin/master
   ```

## 🔐 Предотвращение в будущем:

1. ✅ Используйте `.env` файлы (уже в `.gitignore`)
2. ✅ Используйте переменные окружения в продакшене
3. ✅ Никогда не коммитьте токены, пароли, API ключи
4. ✅ Используйте [GitGuardian](https://www.gitguardian.com/) или подобные инструменты для мониторинга
5. ✅ Проверяйте код перед коммитом: `git diff` перед `git add`

## 📚 Полезные ссылки:

- [Telegram Bot API Security](https://core.telegram.org/bots/api#security)
- [GitGuardian Documentation](https://docs.gitguardian.com/)
- [OWASP Secrets Management](https://owasp.org/www-community/vulnerabilities/Use_of_hard-coded_cryptographic_key)

