# 🚨 СРОЧНЫЕ ДЕЙСТВИЯ - Утечка токена бота

## ⚠️ КРИТИЧЕСКИ ВАЖНО - Выполните СЕЙЧАС:

### 1. Отзовите токен через BotFather (5 минут)

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте: `/revoke`
3. Выберите вашего бота
4. Подтвердите отзыв
5. Отправьте: `/token` для генерации нового токена
6. **Сохраните новый токен!**

### 2. Обновите Railway (2 минуты)

1. Откройте https://railway.app
2. Ваш проект → Settings → Variables
3. Измените `BOT_TOKEN` на новый токен
4. Railway автоматически перезапустит

### 3. Обновите локальный .env (1 минута)

```bash
# Отредактируйте .env
nano .env
# Или
vim .env

# Установите новый токен:
BOT_TOKEN=НОВЫЙ_ТОКЕН_ОТ_BOTFATHER
ALLOWED_USER_ID=7186109787
PORT=3000
```

### 4. Обновите webhook (1 минута)

```bash
export BOT_TOKEN="НОВЫЙ_ТОКЕН_ОТ_BOTFATHER"
./check-webhook.sh https://finbot-production-19c7.up.railway.app
```

## ✅ После выполнения срочных действий:

### Очистка истории Git (можно сделать позже)

Токен все еще в истории коммитов. Для полной очистки:

```bash
# Установите git-filter-repo
pip install git-filter-repo

# Запустите скрипт очистки
./clean-git-history.sh

# Или вручную:
git filter-repo --replace-text <(echo "REDACTED_TELEGRAM_BOT_TOKEN==>YOUR_BOT_TOKEN_HERE")
git push origin --force --all
```

**ВНИМАНИЕ:** Force push перезапишет историю. Убедитесь, что все участники проекта знают об этом.

## 📋 Чеклист:

- [ ] Токен отозван через BotFather
- [ ] Новый токен сгенерирован
- [ ] Railway обновлен с новым токеном
- [ ] Локальный .env обновлен
- [ ] Webhook обновлен
- [ ] Бот работает с новым токеном
- [ ] История Git очищена (можно позже)

## 🔐 Предотвращение в будущем:

- ✅ Всегда используйте `.env` файлы (в `.gitignore`)
- ✅ Используйте переменные окружения в продакшене
- ✅ Никогда не коммитьте секреты
- ✅ Проверяйте `git diff` перед коммитом

